# `/scripts`

Scripts to perform various build, install, analysis, and other operations.

These scripts keep the root-level Makefile small and simple.

## Scripts

- `build.sh` - compiles the `forebay` binary from `./cmd/forebay`.
