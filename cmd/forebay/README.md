# `/cmd/forebay`

Main application for this project.

The directory name for the application matches the name of the executable: `forebay`.

## Build

```bash
go build -o ../../forebay .
```

Or from the repository root:

```bash
go build -o forebay ./cmd/forebay
```
